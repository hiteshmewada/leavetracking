This is java project used to learn break and build project.

Added flyway for data migration script so that every time when some schema changes no need to delete data.

Planning to use dataMapper also and auth jwt also in this.
